spring.datasource.url=jdbc:postgresql://localhost:5432/restaurant
spring.datasource.username=postgres
spring.datasource.password=lol21
spring.datasource.platform=postgres

spring.jpa.hibernate.ddl-auto=create
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.connection.charSet=UTF-8
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.hbm2ddl.import_files=data.sql
spring.jpa.properties.hibernate.hbm2ddl.import_files_sql_extractor=org.hibernate.tool.hbm2ddl.MultipleLinesSqlCommandExtractor

basket.max.size=500
basket.expiration.time=1800

base.module.elementToSearch=1,2,3,4,5,6

patterns.all.users=/, /home, /catalogue, /getDishDetails/*, /registration, /filterByCategory/*, /getAllDishes, \
 /basket/*, /webjars/**, /css/*, /img/*, /js/*, /storage/*, /favicon.ico, /getCurrentUser
patterns.admin=/categoryTree, /manageProducts

notEmpty=This field is required.
size.userForm.username=Please use between 5 and 32 characters.
duplicate.userForm.username=Someone already has that username.
size.userForm.password=Try one with at least 5 characters.
diff.userForm.passwordConfirm=These passwords don't match.

category.has.subcategories.error=Category cannot be deleted because of existing related subcategories.
category.has.products.error=Category cannot be deleted because of existing related products.
deleting.category.error=Error during deleting category.

product.is.ordered.error=This product is ordered by a user.
deleting.product.error=Error during deleting product.
